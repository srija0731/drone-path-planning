services:
  - type: web
    name: drone-path-planning
    runtime: python
    buildCommand: ""
    startCommand: "python server.py"
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: OPEN_BROWSER
        value: "false"
      - key: DRONE_API_TOKEN
        generateValue: true